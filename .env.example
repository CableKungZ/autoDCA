# PostgreSQL
POSTGRES_USER=dca
POSTGRES_PASSWORD=changeme
POSTGRES_DB=autodca

# Redis
REDIS_URL=redis://redis:6379/0

# Encryption key — generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# Binance API (leave empty if not using)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Bitkub API (leave empty if not using)
BITKUB_API_KEY=
BITKUB_API_SECRET=

# Telegram Bot (leave empty to disable notifications)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Ports
API_PORT=8888
FRONTEND_PORT=3333

# Logging
LOG_LEVEL=INFO
